apiVersion: v1
kinda: Service
metadata:
  name: my-app-service
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80
      containerPort: 8080
  type: ClusterIP / NodePort / LoadBalancer
